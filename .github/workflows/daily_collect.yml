name: Daily Blog Collector

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run blog collector
        run: python collect.py

      - name: Commit results
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add outputs/
          git diff --staged --quiet || git commit -m "📰 Daily posts — $(date +'%Y-%m-%d')"
          git push
```

4. **"Commit changes"** → **"Commit changes"**

---

### Check 2: Go Back to Actions Tab

After committing, click **"Actions"** again — it should now appear:
```
All workflows
└── Daily Blog Collector  ✅
